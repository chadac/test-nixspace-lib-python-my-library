[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "my_library"
version = "0.0.1"
description = "Test library"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests",
]